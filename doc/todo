*email list of things to contribute

*Add more language functionality
   - int a = 3
   - for (;;)
*Testing, testing testing
   - while (a < b && c > d || e == f)
   - foo(bar(x), y)

*if, while condition simplification
  node number, binary tree
  && -> 0
  || -> 1
  < -> 2
  
  Leafs : must be comparisons